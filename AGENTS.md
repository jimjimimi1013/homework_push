# AGENTS.md

이 프로젝트는 현재 운영 중인 린중국어 학원 숙제 관리 PWA입니다.

## 기본 작업 원칙

- 사용자가 요청한 부분만 최소 수정합니다.
- 관련 없는 코드, UI, 기능은 건드리지 않습니다.
- 기존에 정상 동작하는 로직은 그대로 유지합니다.
- 기존 구조를 다시 만들거나 대규모 리팩터링하지 않습니다.
- 파일 전체를 새로 작성하지 말고 필요한 코드 영역만 수정합니다.
- 이번 요청과 관계없는 코드 정리, 구조 개선, 공통화, 최적화는 하지 않습니다.
- 다른 영역까지 수정이 필요하다고 판단되면 임의로 진행하지 말고 먼저 사용자에게 알려주세요.

## Git 작업 원칙

- 운영 중인 `main` 브랜치는 직접 수정하지 않습니다.
- 작업마다 별도 브랜치를 만들어 수정합니다.
- 브랜치 이름은 작업 내용을 알 수 있게 간단히 작성합니다.
  - 예: `fix-feedback-ui`
  - 예: `fix-student-message-scroll`
- 작업 후 build까지 확인합니다.
- 사용자가 확인하기 전에는 `main`에 merge하지 않습니다.

## 수정 범위

- 가능하면 현재 source 구조를 그대로 사용합니다.
- `src/`의 실제 source를 수정합니다.
- `dist/` 및 자동 생성 파일은 직접 수정하지 않습니다.
- DB 구조, 라우팅, 알림, PWA/Service Worker 등은 요청이 있을 때만 수정합니다.
- 기존 데이터 형식을 임의로 변경하지 않습니다.

## 검증

수정 후 반드시:

1. `npm run build` 실행
2. build 성공 여부 확인
3. 실제 수정한 파일과 영역을 간단히 보고
4. 요청 범위 외 변경이 없는지 확인

## 중요

현재 구현이 다소 비효율적으로 보여도 이번 요청과 관계없다면 그대로 두세요.

**새로 설계하는 것보다 현재 정상 동작을 유지하는 것을 우선합니다.**
## 기존 UI 재사용 원칙

새 화면이나 새 UI가 필요할 때는 먼저 현재 앱에서 가장 유사한 기존 화면/컴포넌트를 찾아 재사용합니다.

- 같은 목적의 기존 화면이 있으면 새 디자인을 만들지 않습니다.
- 기존 레이아웃, 여백, 폰트, 버튼, 카드, 모달, 헤더, 탭 구조를 그대로 복제하고 내용만 변경하는 방식을 우선합니다.
- 새 CSS, 새 컴포넌트, 새 interaction pattern은 기존 구현으로 해결할 수 없는 경우에만 추가합니다.
- 기존 화면과 비주얼이 달라질 가능성이 있으면 임의로 새 방식으로 구현하지 말고 먼저 보고합니다.
- “더 깔끔하다”, “더 현대적이다” 같은 이유로 기존 UI를 재설계하지 않습니다.
